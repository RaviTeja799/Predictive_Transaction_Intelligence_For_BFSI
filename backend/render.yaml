# Render.com Build Configuration for FastAPI Backend
# https://render.com/docs/deploy-fastapi

# Build command
build:
  - pip install -r requirements.txt

# Start command
start: uvicorn src.api.main:app --host 0.0.0.0 --port $PORT
